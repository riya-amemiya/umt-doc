---
title: parseEmail
---

## About

メールアドレスを解析し、バリデーション結果を返します。`basic`、`rfc822`、`rfc2822`、`rfc5321`、`rfc5322` の各レベルに対応した検証パターンを使用できます。検証に成功した場合、ローカルパートとドメインパートに分割された結果も返します。ReDoS 攻撃を防止するため、過度に長い入力は自動的に拒否されます。
