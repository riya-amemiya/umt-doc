---
title: pipe
---

## About

パイプライン処理を行うための関数です。初期値を受け取り、`Pipe` インスタンスを生成します。`map` で値の変換、`when` で条件付き変換、`tap` で副作用の実行、`filterStrict` / `filterWithDefault` / `filterResult` でフィルタリングを行い、`end` で最終結果を取得できます。関数を連鎖的に適用することで、可読性の高いデータ変換パイプラインを構築できます。
